//
// Created by lamarwells on 6/6/2024.
//
#include <stdio.h>
#include <math.h>

    int main(){

//BASIC MATH FUNTIONS
    /*double A = sqrt(9);
    double B = pow(2,4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.99);
    double F = fabs(-100);
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%d", C);*/
    //Circumference Program

    /*const double PI = 3.14159;
    double radius;
    double circumference;
        printf("\n Enter the radius of a cirlce: ");
        scanf("%lf", &radius);

        circumference = 2 * PI * radius;
        printf("circumference: %lf", circumference)*/

    return 0;

};